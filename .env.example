# Copy to .env and fill in your key. The Express proxy reads this; the key is
# never sent to the browser.
ANTHROPIC_API_KEY=sk-ant-...

# Port the Express proxy listens on (Vite proxies /api here).
PORT=8787
